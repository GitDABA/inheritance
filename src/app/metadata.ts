import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sibling Distribution App',
  description: 'Fairly distribute items among siblings using a weighted priority system',
};
